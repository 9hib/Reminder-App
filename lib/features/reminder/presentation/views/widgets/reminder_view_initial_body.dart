import 'package:flutter/widgets.dart';

class ReminderViewInitialBody extends StatelessWidget {
  const ReminderViewInitialBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Create your first reminder"));
  }
}